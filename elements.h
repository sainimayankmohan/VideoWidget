#ifndef ELEMENTS_H
#define ELEMENTS_H

#include <QWidget>
#include <QCheckBox>
#include <QProgressBar>

class Elements : public QWidget
{
    Q_OBJECT
public:
    explicit Elements(QWidget *parent = 0);

signals:

public slots:

private:

};

#endif // ELEMENTS_H
